import Link from 'next/link';import {login} from '../actions'
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;reset?:string}>}){const q=await searchParams;return <div className="auth panel"><h1>Sign in</h1>{q.reset?<p className="authSuccess">Password updated. Sign in with your new password.</p>:null}{q.error?<p className="authError">Email or password is incorrect.</p>:null}<form action={login}><label>Email</label><input name="email" type="email" autoComplete="email" required/><label>Password</label><input name="password" type="password" autoComplete="current-password" required/><div className="authPasswordRow"><span></span><Link href="/auth/forgot-password">Forgot password?</Link></div><button className="button">Sign in</button></form><p className="muted">No account? <Link href="/auth/register">Register</Link></p></div>}