import Link from 'next/link';import {requestPasswordReset} from '../actions'
export default async function ForgotPassword({searchParams}:{searchParams:Promise<{sent?:string}>}){const q=await searchParams;return <div className="auth panel"><h1>Reset password</h1>{q.sent?<><p className="authSuccess">If an account exists for that email, a password reset link has been sent.</p><p className="muted"><Link href="/auth/login">Back to sign in</Link></p></>:<><p className="muted">Enter your account email and we’ll send you a secure reset link.</p><form action={requestPasswordReset}><label>Email</label><input name="email" type="email" autoComplete="email" required/><button className="button">Send reset link</button></form><p className="muted"><Link href="/auth/login">Back to sign in</Link></p></>}</div>}