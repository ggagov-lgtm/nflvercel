import {updatePassword} from '../actions'
export default async function ResetPassword({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <div className="auth panel"><h1>Choose a new password</h1>{q.error?<p className="authError">Passwords must match and contain at least 8 characters. If the reset link expired, request a new one.</p>:null}<form action={updatePassword}><label>New password</label><input name="password" type="password" minLength={8} autoComplete="new-password" required/><label>Confirm new password</label><input name="confirm" type="password" minLength={8} autoComplete="new-password" required/><button className="button">Update password</button></form></div>}